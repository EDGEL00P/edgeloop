/**
 * Users Domain
 * User management and profile logic
 */

// Placeholder - will extract user management from server code
export interface User {
  id: string;
  email: string | null;
  firstName: string | null;
  lastName: string | null;
  profileImageUrl: string | null;
}