{
  "name": "@edgeloop/core",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@edgeloop/db": "workspace:*",
    "@edgeloop/shared": "workspace:*",
    "drizzle-orm": "^0.38.3"
  }
}
