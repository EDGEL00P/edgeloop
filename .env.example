
# Example environment variables for EdgeLoop

# Server configuration
PORT=3000
HOST=0.0.0.0
SHUTDOWN_GRACE_MS=5000

# Vercel deployment (set as GitHub secrets for CI/CD)
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=

# Railway deployment (set as GitHub secret for CI/CD)
RAILWAY_TOKEN=

# Codecov upload token (set as GitHub secret for CI/CD)
CODECOV_TOKEN=

# Add any additional environment variables required by your packages below

# ============================================
# MCP Server Configuration (Free Tier Only)
# ============================================
# These environment variables are used by MCP (Model Context Protocol) servers
# configured in .claude/claude_desktop_config.json
# All servers are 100% FREE with no paid APIs required!

# Repository Path (REQUIRED for filesystem, git, and sqlite servers)
# Set this to the absolute path of your edgeloop repository clone
# Example (macOS/Linux): /Users/yourname/projects/edgeloop
# Example (Windows): C:\Users\yourname\projects\edgeloop
EDGELOOP_REPO_PATH=/path/to/your/edgeloop/repository

# GitHub MCP Server (optional - for GitHub integration)
# Get token at: https://github.com/settings/tokens (completely free)
# Required scopes: repo, workflow, read:org
GITHUB_TOKEN=

# All other MCP servers work without any environment variables!
# - memory: FREE, no config needed
# - playwright: FREE, no config needed (already a dev dependency)
# - puppeteer: FREE, no config needed
# - sequential-thinking: FREE, no config needed
# - fetch: FREE, no config needed
# - time: FREE, no config needed
